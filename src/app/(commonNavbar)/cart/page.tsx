import ShoppingCart from "@/components/Shared/Navbar/components/ShoppingCart";
import { Container } from "@mui/material";

const page = () => {
  return (
    <Container>
      <ShoppingCart />
    </Container>
  );
};

export default page;
